# It's highly recommended to install PyTorch from the official website
# to ensure compatibility with your CUDA or MPS setup:
# https://pytorch.org/get-started/locally/
torch>=2.0.0
pandas>=1.5.0
numpy>=1.24.0
PyQt6>=6.4.0
pyqtgraph>=0.13.0
scikit-learn>=1.2.0
tqdm>=4.64.0
pandas-ta>=0.3.14b
# Data storage and compression
pyarrow>=10.0.0  # For parquet file support
# Performance optimization
numba>=0.57.0  # For JIT compilation acceleration
# Logging and monitoring (optional)
tensorboard>=2.11.0
wandb>=0.13.0  # Weights & Biases (optional, set ENABLE_WANDB=True in config)
# Development and debugging
matplotlib>=3.6.0  # For additional plotting capabilities
seaborn>=0.12.0  # For statistical visualizations
plotly>=5.12.0  # For interactive plots (alternative to matplotlib)
# Data validation and testing
pytest>=7.2.0  # For unit testing
pytest-cov>=4.0.0  # For coverage reports 